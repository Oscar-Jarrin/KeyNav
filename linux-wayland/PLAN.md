0. Specify the architecture for the configuration files managing 

1. Specify the stack I am going to use for each module
    - Input Listener: evdev
    - Bind travker: pure python
    - The config State: json
    - Keynav State/Mouse State pattern: pure python
    - Event Bus: pyqt6 slots
    - GUI: pyqt/ pyside

2. The first problem to board is going to be the configuration GUI. Managing the input, moving in the GUI, and also typing a bind (at least at first)
